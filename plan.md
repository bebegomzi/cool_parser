# cool_parser 인계 계획

## 목적

이 프로젝트는 CoolMessenger `.udb` 메시지 DB를 읽어, 첨부파일 없이 메시지 텍스트 중심으로 검색하고 확인할 수 있는 도구를 만든 뒤, 최종적으로는 `.udb` 사본을 안전하게 수정, 삭제, 추가할 수 있는 도구를 만드는 것을 목표로 한다.

우선순위는 다음과 같다.

1. 검색 가능한 별도 아카이브와 뷰어를 만든다.
2. 원래 프로그램에서 열리는 `.udb` 편집 가능성을 검토한다.
   1. 다른 메시지함의 기존 메시지를 가져올 수 있는지 검토한다.
   2. 필요 없는 메시지를 삭제할 수 있는지 검토한다.
3. 매번 LLM의 도움을 받지 않아도 되도록, 파싱 모듈, gui 기반 메시지함 수정/ 삭제/추가 모듈을 구안한다.
4. 향후 Openclaw나 Hermes Harness와 같은 에이전트에게 업무 처리 자동화를 부탁하도록 메시지함의 규칙을 분석하여 정리한다.
5. 안 읽은 메시지를 AI가 요약하고, 할 일, 회신 필요 항목, 일정 후보를 정리하는 CoolMessenger 기반 개인 비서 기능을 만든다.

원래 프로그램용 `.udb` 수정 실험은 현재 뷰어와 파싱 흐름이 안정화된 뒤, 작업 폴더에 둔 사본에서만 별도 실험으로 진행한다.

## 확인된 구조

`.udb` 파일은 SQLite 3 데이터베이스다.

핵심 테이블은 다음과 같다.

- `tbl_recv`: 받은 메시지
- `tbl_send`: 보낸 메시지
- `tbl_member`: 구성원 목록

보조 테이블은 다음과 같다.

- `tbl_dbInfo`: DB 정보
- `tbl_alarm`: 알림 정보
- `tbl_autotext`: 자동 문구

메시지 본문은 `MessageText`를 우선 사용한다. `MessageBody`는 `{COMP}...` 형태의 압축 또는 서식 포함 원문일 가능성이 있으므로, 필요하면 보존하되 1차 아카이브의 주 본문으로 쓰지 않는다.

## 현재 관찰 결과

손상된 예전 DB에서는 `PRAGMA integrity_check` 실행 시 페이지 손상 오류가 발생했다. 일반적인 테이블 전체 조회는 중간에 실패할 수 있지만, `MessageKey` 단위로 한 행씩 조회하면 더 많은 메시지를 복구할 수 있었다.

손상 DB와 정상 새 DB 사본 기준으로 확인된 복구 결과는 다음과 같다.

- 손상 DB `tbl_recv`: 전체 5,690건 중 5,579건 복구, 111건 실패
- 손상 DB `tbl_send`: 2,168건 복구
- 정상 새 DB `tbl_recv`: 418건
- 정상 새 DB `tbl_send`: 113건
- 정상 새 DB `tbl_member`: 74건

두 DB의 테이블 구조는 거의 동일하다. 따라서 구조 변환보다는 손상 행 우회 복구와 병합 정책이 핵심이다.

라이브 CoolMessenger DB 기준으로는 현재 DB를 임시 복사한 뒤 읽기 전용으로 파싱하는 흐름을 확인했다.

- 라이브 DB `tbl_recv`: 423건
- 라이브 DB `tbl_send`: 116건
- 라이브 DB 파싱 실패: 0건

## 구현 완료

현재 구현된 파일은 다음과 같다.

- `scripts/app.py`: 배포판용 웹 시작 화면과 뷰어 서버를 실행한다.
- `scripts/merge_messages.py`: `.udb`를 읽어 통합 SQLite DB를 만든다.
- `scripts/viewer.py`: 통합 SQLite DB를 브라우저에서 검색하고 확인한다.
- `scripts/run.py`: 기본 실행 진입점이다.
- `scripts/search_messages.py`: 터미널에서 간단히 검색할 때 쓰는 보조 도구다.

기본 실행은 다음과 같다.

```powershell
python scripts\run.py
```

기본 실행 흐름은 다음과 같다.

1. `%LOCALAPPDATA%\CoolMessenger\Memo.` 폴더에서 `*.udb` 파일을 찾아 임시 폴더로 복사한다.
2. 임시 복사본을 읽기 전용으로 파싱한다.
3. `output/run_타임스탬프/merged_messages.sqlite`를 만든다.
4. 로컬 웹 뷰어를 실행한다.
5. 뷰어가 종료되면 임시 `.udb`와 임시 작업 폴더를 정리한다.

파싱 결과를 남기고 싶으면 다음 옵션을 사용한다.

```powershell
python scripts\run.py --keep-work-dir
```

이미 만들어진 SQLite 결과를 바로 열려면 다음 명령을 사용한다.

```powershell
python scripts\run.py --use-existing
```

작업 폴더에 둔 `.udb` 사본을 읽으려면 다음 명령을 사용한다.

```powershell
python scripts\run.py --backup
```

이 경우 기본 입력 폴더는 `input`이다. 현재 예전 사본 폴더인 `sources`를 직접 지정할 수도 있다.

```powershell
python scripts\run.py --backup --input-dir sources
```

현재 뷰어 기능은 다음과 같다.

- 검색어, DB 구분, 받은/보낸 메시지 필터
- 공백으로 구분한 여러 키워드의 AND 검색
- 100건 단위 페이지 이동
- 받은 메시지와 보낸 메시지의 배경색 구분
- 안 읽은 받은 메시지를 목록 최상단에 표시
- 안 읽은 메시지는 연한 파란 배경과 진한 파란 띠, 굵은 글꼴로 표시
- 읽은 메시지는 일반 글꼴로 표시
- 본문 안의 `OO님이 보낸글 >>YYYY/MM/DD ...` 인용 구조를 감지
- 좌측 목록에서 인용 체인 메시지를 대화 단위로 묶어 표시
- 우측 상세 화면에서 카카오톡식 말풍선으로 대화 표시
- 보낸 메시지는 오른쪽 노란 말풍선, 받은 메시지는 왼쪽 흰 말풍선
- 최신 메시지는 우측 상세 화면의 아래쪽에 표시
- `tbl_member`를 이용해 받은 사람 내부 번호를 가능한 이름으로 변환

배포판은 PyInstaller 폴더 배포 방식으로 빌드한다. 배포 zip에는 `cool_parser.exe`, `input`, `output`, `README.md`를 포함한다. Python이 설치되지 않은 Windows 환경에서도 실행하는 것을 목표로 한다.

CSV 파일은 뷰어용 필수 중간 파일이 아니다. 뷰어는 SQLite를 읽는다. CSV는 사람이 엑셀 등으로 검산하거나 외부 도구에 넘기기 위한 보조 산출물이다.

## 다음 작업

현재 기본 뷰어는 `.udb`를 수정하지 않는다. 라이브 DB는 임시 복사본을 만들어 읽기 전용으로 파싱한다.

앞으로의 핵심 작업은 `.udb`에 다른 메시지함의 기존 메시지를 가져오고, 필요 없는 메시지를 삭제할 수 있는지 검토하는 것이다. 작업 폴더에 들어오는 `.udb`는 원본이 아니라 사본으로 간주하여 바로 수정하고, 현재 CoolMessenger DB를 직접 수정할 때만 첫 수정 전 자동 백업과 변경 로그를 남긴다.

통합용 별도 SQLite DB를 만든다. 기본 파일명은 `merged_messages.sqlite`로 둔다.

예전 DB와 새 DB의 메시지는 공통 `messages` 테이블로 정규화한다. 주요 필드는 다음과 같다.

- `source_db`: 예전 DB 또는 새 DB 구분
- `box`: `recv` 또는 `send`
- `original_key`: 원래 DB의 `MessageKey`
- `date`: 메시지 날짜
- `title`: 제목
- `person`: 검색과 목록 표시용 상대방 이름
- `sender`: 보낸 사람
- `receiver`: 받은 사람
- `body_text`: 사람이 읽을 본문 텍스트
- `deleted_date`: 삭제일
- `is_unread`: 읽지 않은 받은 메시지 여부
- `recovery_status`: `ok`, `partial`, `failed` 같은 복구 상태

손상으로 읽지 못한 `MessageKey`는 별도 보고서로 남긴다.

중복 후보는 날짜, 제목, 본문 일부, 상대방 기준으로 탐지한다. 자동 삭제하지 말고, 사람이 확인할 수 있는 후보 목록만 만든다.

다음 구현 후보는 다음과 같다.

1. `.udb` 사본에 다른 메시지함의 메시지 1건을 가져온 뒤 원래 프로그램에서 정상 표시되는지 확인한다.
2. `.udb` 사본에서 메시지 1건을 물리 삭제한 뒤 원래 프로그램 반응을 확인한다.
3. 여러 메시지를 필터링한 뒤 선택 삭제하는 UI를 설계한다.
4. 수정 전 자동 백업, 변경 로그, 되돌리기 전략을 정한다.
5. 편집 GUI에서는 임의 새 메시지 생성이 아니라 기존 메시지 가져오기와 선택 삭제를 핵심 기능으로 둔다.
6. 검색 기능을 고도화한다.
   - 예: `서광민 예산`처럼 여러 키워드를 조합해 검색한다.
   - 예: `지난 주 서광민이 보낸 예산 관련 내용`처럼 자연어 조건을 날짜, 사람, 보낸/받은 방향, 키워드로 해석한다.
   - 우선은 규칙 기반 파싱으로 시작하고, 필요하면 나중에 임베딩 또는 로컬 검색 인덱스를 검토한다.
7. CoolMessenger 기반 개인 비서 기능을 설계한다.
   - 안 읽은 메시지를 우선 추출한다.
   - 본문 안의 인용 구조를 분리해 실제 새로 온 내용과 이전 대화 맥락을 구분한다.
   - AI가 메시지를 요약하고, 할 일, 회신 필요 항목, 마감, 일정 후보를 추출한다.
   - 추출 결과를 Openclaw, Hermes Harness 같은 에이전트가 읽기 쉬운 JSON, SQLite, API 형태로 제공하는 방식을 검토한다.
   - 구형 CoolMessenger를 사설 이메일 서비스의 AI 비서 기능처럼 보완하는 것을 장기 목표로 둔다.

## 구현 방향

복구 스크립트는 읽기 전용 연결을 기본으로 한다.

손상 DB는 테이블 전체 조회가 실패할 수 있으므로, `MessageKey`의 최소값과 최대값을 구한 뒤 각 키를 개별 조회한다. 실패한 키는 건너뛰고 보고서에 기록한다.

첨부파일 관련 컬럼은 이번 목표에서 제외한다. `FilePath`, `FileHost`, `CoolFile2SessionID`, `LinkURL` 등은 1차 통합 대상이 아니다.

날짜 끝의 요일 표기가 깨져 보일 수 있으므로 날짜 본체 파싱을 우선한다. 예를 들어 `2026/05/14 13:32:59 (...)` 형태에서는 앞의 날짜와 시간 부분을 우선 사용한다.

CSV는 셀 내부 줄바꿈이 정상적으로 포함될 수 있다. 줄 단위로 직접 파싱하지 말고 Python `csv` 모듈, pandas, SQLite import 등 표준 CSV 파서를 사용한다.

## 주의사항

이 문서는 실행 로그가 아니라 새 세션 인계를 위한 핸드아웃이다.

라이브 DB 경로는 Windows 기본 위치만 기록한다. 개인별 일회성 경로, GitHub 계정, 민감한 메시지 내용은 기록하지 않는다.

첨부파일 복구는 제외한다.

원래 프로그램에서 다시 열리는 `.udb` 수정 작업은 가능성을 검토하되, 라이브 원본이 아니라 사본에서만 진행한다.
